#include "main.h"

/**
 * main - tests function that prints if integer is positive, negative, or zero
 * Return: 0
 */
int main(void)
{
	/* Declare and initialize an integer variable to hold the test value. */
	int i = 0;

	positive_or_negative(i);
	return (0);
}
